{
  "name": "azure-static-web-apps-api",
  "version": "1.0.0",
  "description": "Azure Functions API for file redirects and ActivityPub federation in Azure Static Web Apps",
  "main": "files/index.js",
  "scripts": {
    "start": "func start",
    "build": "tsc -p ../tsconfig.api.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@azure/functions": "^4.5.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "keywords": [
    "azure-functions",
    "azure-static-web-apps",
    "redirect",
    "activitypub",
    "federation",
    "blob-storage"
  ]
}
